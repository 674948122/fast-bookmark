I will fix the issue where the sidebar title (`extensionName`) is not updating dynamically when the language changes.

**The Problem:**
Although I changed the initial HTML generation to use `${getMsg("extensionName")}`, I missed adding the `data-i18n="extensionName"` attribute to the `<h2>` element.
The `updateTexts()` function relies on `querySelectorAll("[data-i18n]")` to find elements that need translation updates. Since this element lacks the attribute, it is ignored during the update cycle.

**The Solution:**

1. Add `data-i18n="extensionName"` to the \`#fast-bookmark-sidebar-title

