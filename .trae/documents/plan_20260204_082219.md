I will add a configuration option to control the sidebar opening direction (Left or Right), defaulting to Right.

**Implementation Details:**

1. **Internationalization (i18n):**

   * Add keys for "Position" (`positionLabel`), "Left" (`positionLeft`), and "Right" (`positionRight`) to `_locales/en/messages.json` and `_locales/zh_CN/messages.json`.

2. **Settings Management (`content.js`):**

   * Update the `settings` object to include a `position` property, defaulting to `"right"`.

   * Update `loadSettings` to retrieve this value.

   * Update the save logic to persist this value.

3. **UI Updates (`content.js`):**

   * **Settings Modal**: Add a radio button group (or select dropdown) for "Position" in the settings modal.

   * **CSS Styles**:

     * Modify `#fast-bookmark-modal` styles to support dynamic positioning.

     * Create specific styles for left (`left: 0`, `transform: translateX(-100%)`) vs right (`right: 0`, `left: auto`, `transform: translateX(100%)`) positioning.

     * Update the animation logic (`slideIn` / `transform`) to respect the chosen direction.

4. **Logic (`content.js`):**

   * In `updateStyles`, dynamically set the CSS variables or class names based on `settings.position`.

   * Ensure the `toggle` function respects the position when showing/hiding the sidebar.

**Plan Steps:**

1. Update `_locales` files.
2. Update `content.js` HTML to include the position setting control.
3. Update `content.js` logic to handle saving/loading the position setting.
4. Update `content.js` CSS injection to apply the correct styles based on position.

