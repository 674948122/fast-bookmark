I will implement a "Launcher Wallpaper" feature that allows users to upload a custom background image for the launcher page.

1.  **Explain Limitation**: I will clarify that browser extensions cannot automatically read the user's desktop wallpaper due to security sandboxing, so a manual upload is the standard solution.

2.  **Update Localization**:
    -   Add translation keys (`wallpaperLabel`, `uploadButton`, `clearButton`, `wallpaperSizeError`) to `_locales/en/messages.json` and `_locales/zh_CN/messages.json`.

3.  **Modify `content.js`**:
    -   **Apply Wallpaper**: On script initialization, if `isLauncher` is true, fetch the `launcherWallpaper` from `chrome.storage.local` and apply it to `document.body.style.backgroundImage`.
    -   **Update Settings UI**: Add a new row in the settings modal for "Launcher Wallpaper" with an "Upload" button (hidden file input) and a "Clear" button.
    -   **Handle Upload**: Implement logic to read the selected image file (limit to 4MB), convert it to a Base64 Data URL, and save it to `chrome.storage.local`.
    -   **Handle Clear**: Implement logic to remove the wallpaper from storage and reset the background.

This approach provides the desired visual customization while respecting browser security constraints.