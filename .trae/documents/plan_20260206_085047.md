I will fix the issue where the sort order change doesn't take effect immediately.

**Plan:**

1. **Update** **`content.js`**:

   * Locate the `saveBtn.addEventListener("click", ...)` block (lines 1009-1041).

   * In the callback function of `chrome.storage.sync.set`, add a call to `fetchBookmarks()`.

   * This ensures that after the settings (including `sortOrder`) are saved, the bookmarks are re-fetched and re-sorted according to the new configuration.

**Current Code:**

```javascript
            chrome.storage.sync.set(
                {
                    // ...
                },
                () => {
                    settingsModal.style.display = "none";
                    updateStyles();
                    updateTexts();
                },
            );
```

**New Code:**

```javascript
            chrome.storage.sync.set(
                {
                    // ...
                },
                () => {
                    settingsModal.style.display = "none";
                    updateStyles();
                    updateTexts();
                    fetchBookmarks(); // Add this line
                },
            );
```

