I will fix the missing text labels in the Settings menu by adding the missing translation keys to the extension's locale files.

**Reason for Issue:**
The "Sort Order" feature relies on `chrome.i18n` when the language is set to "Auto" (default). Since I previously only updated the internal dictionary in `content.js` but not the actual `messages.json` files used by Chrome, the text lookups were failing, resulting in empty labels.

**Plan:**

1.  **Update `_locales/en/messages.json`**:
    *   Add `sortOrderLabel`: "Sort Order"
    *   Add `sortDefault`: "Default"
    *   Add `sortFrequency`: "Most Visited"

2.  **Update `_locales/zh_CN/messages.json`**:
    *   Add `sortOrderLabel`: "排序规则"
    *   Add `sortDefault`: "默认排序"
    *   Add `sortFrequency`: "根据访问频率"

This will ensure the text appears correctly for all users.