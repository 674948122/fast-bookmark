To address the user's request, I will modify `content.js` to change how the shortcut key is displayed to the user, replacing "Meta" with "Command" (on Mac) or "Ctrl" (on Windows/Linux) for better clarity, while internally keeping the standard key names.

**Plan:**

1.  **Helper Function for Display**:
    *   Create a helper function `formatShortcutForDisplay(shortcut)` in `content.js`.
    *   This function will take the internal shortcut string (e.g., `meta+b`, `ctrl+b`) and format it for user display:
        *   Replace `meta` with `Command` (or `Cmd`).
        *   Replace `ctrl` with `Ctrl`.
        *   Capitalize other keys.
        *   Join with `+`.

2.  **Update `content.js`**:
    *   Locate where `shortcutInput.textContent` is set.
        *   Inside the `configBtn` click listener (initial open).
        *   Inside the `shortcutInput` keydown listener (recording).
    *   Use the new `formatShortcutForDisplay` function when setting `textContent`.
    *   Ensure that `tempShortcut` (used for saving) keeps the internal format (e.g., `meta+b`), while the UI shows the user-friendly format.

3.  **Specific Changes**:
    *   **Initial Display**: In the `configBtn` event listener, change `shortcutInput.textContent = settings.shortcut;` to use the formatter.
    *   **Recording**: In the `shortcutInput` `keydown` listener:
        *   When `e.metaKey` is true, push "Meta" to the internal `keys` array, but when updating the UI text, display "Command".
        *   Actually, a cleaner approach is to keep the `keys` array as is (with "Meta"), and just format the final string before displaying it.

**Implementation Details**:

*   **`formatShortcutForDisplay(shortcut)`**:
    ```javascript
    function formatShortcutForDisplay(shortcut) {
        if (!shortcut) return "";
        return shortcut.split("+").map(key => {
            if (key.toLowerCase() === "meta") return "Command";
            if (key.toLowerCase() === "ctrl") return "Ctrl"; // Or just capitalize
            return key.charAt(0).toUpperCase() + key.slice(1);
        }).join("+");
    }
    ```

*   **Recording Logic Update**:
    Currently, the recording logic pushes "Meta", "Ctrl", etc., directly to the `keys` array and joins them for display.
    I will modify it to:
    1.  Construct the internal `tempShortcut` (e.g., `meta+b`).
    2.  Use `formatShortcutForDisplay(tempShortcut)` to update `shortcutInput.textContent`.

This ensures the user sees "Command+B" but the system saves "meta+b".

**Verification**:
*   Check if `shortcutInput` displays "Command+B" on Mac when opening settings.
*   Check if recording a new shortcut (e.g., Command+Shift+K) displays correctly as "Command+Shift+K".
*   Ensure the saved value remains compatible with the event listener logic (which expects "meta").

I will implement these changes in `content.js`.
