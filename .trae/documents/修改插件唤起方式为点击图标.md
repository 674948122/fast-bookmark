## 修改唤起逻辑为“点击图标”

根据您的需求，我将把插件的激活方式从键盘快捷键改为点击浏览器右上角的插件图标。

### 1. 修改 Manifest 配置
- 从 [manifest.json](file:///Users/pandegong/my-project/fast-bookmark/manifest.json) 中移除 `commands` 字段，不再注册 `Alt+B` 或 `Cmd+B` 快捷键。

### 2. 更新后台脚本逻辑
- 在 [background.js](file:///Users/pandegong/my-project/fast-bookmark/background.js) 中：
    - 移除 `chrome.commands.onCommand` 监听器。
    - 增加 `chrome.action.onClicked` 监听器。当用户点击插件图标时，向当前活动标签页发送 `toggle` 消息。

### 3. 同步更新文档
- 修改 [README.md](file:///Users/pandegong/my-project/fast-bookmark/README.md) 中的“使用指南”部分，明确说明现在通过点击图标来唤起或关闭搜索栏。

是否同意此修改？同意后我将立即实施。