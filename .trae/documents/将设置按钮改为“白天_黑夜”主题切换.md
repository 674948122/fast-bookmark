## 变更目标
- 将侧栏标题右侧的 Settings 图标（[content.js:L377-L382](file:///Users/pandegong/my-project/fast-bookmark/content.js#L377-L382)）改为主题切换按钮：点击在“light ↔ dark”之间切换并立即生效。
- 保留现有样式机制（updateStyles 通过 settings.theme 控制 CSS 变量），但从按钮直接切换而非进入设置页。

## 改动范围
- HTML 模板：替换该处 SVG 为“太阳/月亮”图标，并保留 id=settings-btn 用于绑定。
- 样式：调整 #settings-btn 的显示为可见（当前为 display: none，参考 [content.js:L117-L125](file:///Users/pandegong/my-project/fast-bookmark/content.js#L117-L125)）。
- 逻辑：
  - 新增 updateThemeToggleIcon()：根据 settings.theme 选择显示太阳或月亮。
  - 修改点击事件：不再打开 options.html，改为在 'light' 与 'dark' 间切换；若当前为 'auto'，第一次点击根据系统配色选择相反显式值。
  - 持久化：使用 chrome.storage.sync.set({ theme }) 保存选择；调用 updateStyles() 立即应用。

## 实现细节
1. 模板替换
   - 将 L377-L382 的 SVG 替换为一个可表示两种状态的图标（默认根据 settings.theme 显示），title 改为 "Theme"。
2. 样式修正
   - 把 #settings-btn 的 display 改为 flex，保持交互与悬停效果。
3. 事件绑定
   - 在现有 settingsBtn 绑定位置（[content.js:L411-L421](file:///Users/pandegong/my-project/fast-bookmark/content.js#L411-L421)）改为：
     - 计算 nextTheme：若为 'dark'→'light'；'light'→'dark'；'auto'→按系统偏好选择相反显式值。
     - chrome.storage.sync.set({ theme: nextTheme })；更新 settings.theme；调用 updateStyles() 与 updateThemeToggleIcon()。

## 验证步骤
- 重新加载扩展后：点击按钮可在明暗模式间切换，样式变量与配色即时更新。
- 刷新页面后仍保持选择（验证 storage 持久化）。
- 在系统为暗/亮模式、settings 为 'auto' 时，第一次点击能够切到显式相反模式。

## 风险与回滚
- 改动局部、低风险；如需恢复为打开设置页，将点击处理改回 chrome.runtime.openOptionsPage 即可。
- 不影响其他功能（搜索、树视图、快捷键）。