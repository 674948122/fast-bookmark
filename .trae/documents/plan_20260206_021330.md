I will optimize the UI for the highlight color settings by combining the "Light" and "Dark" theme color pickers into a single row.

1.  **Update Localization**:
    -   Add a new translation key `highlightColorLabel` ("Highlight Color" / "高亮色") to `_locales/en/messages.json` and `_locales/zh_CN/messages.json`.

2.  **Modify `content.js`**:
    -   Update the `i18n` object to include the new `highlightColorLabel` key.
    -   Replace the two separate `.settings-row` divs for light and dark colors with a single row.
    -   Inside this row, place the label "Highlight Color".
    -   Use a flex container to display the two color inputs side-by-side, each with a small label ("Light"/"Dark" using existing `themeLight`/`themeDark` keys).

This change will make the settings interface more compact and visually cleaner.