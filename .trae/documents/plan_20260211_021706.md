# Settings Modal Scrollable Layout Plan

I will modify the settings modal to support scrolling for the configuration list, ensuring the "Save" and "Cancel" buttons remain visible at the bottom.

## 1. Update CSS (`content.js`)
- **`#fast-bookmark-settings-modal`**: Remove the global `padding: 32px`.
- **New `.settings-scroll-container`**:
  - `flex: 1` (Take up remaining vertical space)
  - `overflow-y: auto` (Enable vertical scrolling)
  - `padding: 0 32px` (Restore horizontal padding for content)
  - `min-height: 0` (Allow shrinking in flex container)
- **Settings Title (`h2`)**: Add `padding: 32px 32px 0 32px` and `flex-shrink: 0`.
- **`.settings-actions`**: Add `padding: 24px 32px 32px 32px`, `flex-shrink: 0`, and `background: var(--bg-color)` (to ensure clear separation).

## 2. Update HTML Structure (`content.js`)
- **Wrap Content**: Enclose all `<div class="settings-row">...</div>` elements within a new `<div class="settings-scroll-container">`.
- **Adjust Header**: Ensure the `h2` title is outside the scroll container (at the top).
- **Adjust Actions**: Ensure `.settings-actions` is outside the scroll container (at the bottom).

## 3. Verification
- Open settings modal.
- Verify that the title is fixed at the top.
- Verify that the buttons are fixed at the bottom.
- Verify that the list of settings scrolls correctly in the middle.
- Check layout on different panel widths if necessary (though flex handles this).
