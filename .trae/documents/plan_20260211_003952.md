I will optimize the UI style of the native `<select>` dropdown controls in `content.js` to match the Apple/macOS aesthetic.

**Specific changes to `content.js`:**

1.  **Update `.form-select` CSS:**
    *   Add `appearance: none;` (and `-webkit-appearance: none;`) to remove the default operating system styling.
    *   Inject a custom **Chevron/Arrow Icon** using a Base64/URL-encoded SVG as the `background-image`. This ensures a consistent, clean look across platforms.
    *   Adjust `padding-right` to prevent text from overlapping with the new custom arrow.
    *   Refine `border-radius`, `box-shadow`, and `background-color` to mimic the clean, subtle depth of macOS controls (e.g., using `0 1px 2px rgba(0,0,0,0.1)`).
    *   Enhance the focus state to have a distinct, Apple-style blue ring (using `box-shadow`).

**Implementation Details:**
*   **Icon:** A simple, thin, dark/light adaptive chevron (e.g., `<svg><polyline points="6 9 12 15 18 9"/></svg>`).
*   **Theme Adaptation:** Ensure the arrow icon color adapts to Light/Dark mode (using CSS variables or separate SVG data URIs if necessary, or simply a neutral opacity).

This will result in a modern, consistent dropdown experience that aligns with the rest of the glassmorphism UI.