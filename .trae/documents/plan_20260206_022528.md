I will add Sun and Moon icons to the highlight color settings to make the UI more intuitive.

1. **Modify** **`content.js`**:

   * Locate the "Highlight Color" settings row where I previously combined the inputs.

   * Insert an SVG "Sun" icon (for Light mode) next to the `themeLight` label.

   * Insert an SVG "Moon" icon (for

