# Replace Checkbox with Toggle Switch Plan

I will replace the native checkbox for "Show Recently Visited" with a modern toggle switch (iOS-style switch) to match the "Toggle Shortcut" style or just generally look better.

## 1. Update CSS (`content.js`)
- Add CSS classes for a toggle switch:
  - `.toggle-switch`: The container.
  - `.toggle-input`: The hidden checkbox input.
  - `.toggle-slider`: The visual slider/knob.
  - Styles for checked state (changing background color) and slider position.
  - Styles for focus/hover states.

## 2. Update HTML (`content.js`)
- Replace the existing checkbox `<input type="checkbox">` structure with the new toggle switch structure:
  ```html
  <label class="toggle-switch">
    <input type="checkbox" id="show-recent-checkbox">
    <span class="toggle-slider"></span>
  </label>
  <span class="settings-label-text" data-i18n="showRecentLabel">${getMsg("showRecentLabel")}</span>
  ```
- Adjust the parent container layout to align the label text and the switch (flex row, space-between or similar).

## 3. Verification
- Open settings modal.
- Verify the new toggle switch appearance.
- Verify clicking it toggles the state.
- Verify the setting is saved and applied correctly (logic remains the same, just UI change).
